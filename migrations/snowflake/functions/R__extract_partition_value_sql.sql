CREATE OR REPLACE FUNCTION COM.EXTRACT_PARTITION_VALUE_SQL("METADATAFILE_NAME" VARCHAR(16777216), "PARTITION_COL" VARCHAR(16777216))
RETURNS VARCHAR(16777216)
LANGUAGE SQL
AS $$
	select split(split(METADATAFILE_NAME,PARTITION_COL||'=')[1],'/')[0]::VARCHAR
$$;